annotated tag (add more description)
    git tag -a v1.4 -m "This is an annotated tag" _> stored into db 

Simple tag: 
    git tag v1.4  # this is siomple temporary tag not saved into database
    
    
    $ git tag -d v1.4-lw
Deleted tag 'v1.4-lw' (was e7d5add)